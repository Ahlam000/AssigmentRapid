import React from 'react';
import {View, Text} from 'react-native';

const OrderScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{color: 'black', fontSize: 16}}>Hello Order Screen</Text>
    </View>
  );
};
export default OrderScreen;
